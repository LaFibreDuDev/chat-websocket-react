services:
  frontend:
    build:
      context: ./frontend
    environment:
      VITE_SOCKET_URI: ${VITE_SOCKET_URI}
    ports:
      - '5173:5173'
  backend:
    build:
      context: ./backend
    ports:
      - '3000:3000'